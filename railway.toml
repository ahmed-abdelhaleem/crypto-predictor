[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm --force && pnpm install --no-frozen-lockfile && pnpm run build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
